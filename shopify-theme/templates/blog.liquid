<div class="wrap" style="padding: 170px 24px 100px;">
  <h1 class="display" style="font-size: clamp(2rem, 5vw, 3.5rem);">{{ blog.title }}</h1>
  {% for article in blog.articles %}
    <p><a href="{{ article.url }}">{{ article.title }}</a></p>
  {% endfor %}
</div>
