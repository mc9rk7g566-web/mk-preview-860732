<div class="wrap" style="padding: 170px 24px 100px;">
  <h1 class="display" style="font-size: clamp(2rem, 5vw, 3.5rem);">Collecties</h1>
  {% for collection in collections %}
    <p><a href="{{ collection.url }}">{{ collection.title }}</a></p>
  {% endfor %}
</div>
